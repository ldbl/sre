# Frontend (Vue)

This directory will host the Vue.js single-page application that interacts with the Go backend API.

## TODO
- Initialize a Vue 3 project scaffold (Vite-based) with shared component library.
- Configure API client pointing to the backend service.
- Add unit and e2e testing harness (Vitest + Playwright).
