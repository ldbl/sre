---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: backend
  namespace: production
spec:
  imageRepositoryRef:
    name: backend
    namespace: flux-system
  policy:
    alphabetical:
      order: asc
  filterTags:
    pattern: '^production$'
